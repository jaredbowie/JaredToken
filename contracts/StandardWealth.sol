pragma solidity ^0.4.8;

// things we want to returned
//some integer value times visited
//some decminal value bank balance
//some boolean value
//some string value name/company name etc
//so we don't know what it will returned
// but we could ask what value it will return or write functions indicating the value it will return
//the function name or a specific function can return what the asset is


contract StandardWealth {
  mapping (address => uint256) public balanceOf;

  
}
